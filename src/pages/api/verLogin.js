import users from '../data/lista_usuario'

    const user = props.user
    const password = props.password
    // function verifica() {
    //     if ((user === users.some(login)) && (password === )) {

    //     }
    // }
    console.log(users)